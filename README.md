# FreeRecipe
## Because recipes aren't meant to be secret.

---

Dev workflow:
```docker compose up postgres```
API/ ```dotnet run```
Client/ ```ng serve```

Major version:

```docker compose up --build```